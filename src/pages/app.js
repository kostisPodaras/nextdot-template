import React from 'react';

import { UISetup, App, Layout } from 'components';

const IndexPage = () => (
  <UISetup>
    <Layout>
      <App />
    </Layout>
  </UISetup>
);

export default IndexPage;
